# 03 · 验证清单

按顺序做，**每一步都过了再进下一步**。跳步的话出了问题不知道是哪一层。

> 第 0 步（装依赖 + 类型检查 + 冒烟测试）**已经实测通过**，记录见下。
> 从第 1 步开始需要在真实环境里跑。

---

## 第 0 步 · 装依赖 + 类型检查 + 冒烟测试 ✅ 已通过

```bash
cd /Users/guci/work/edgeone-agent-lab

npm install
npm run typecheck
npm test
```

**实测结果**：

| 命令 | 结果 |
|---|---|
| `npm install` | `added 104 packages in 1m` |
| `npm run typecheck` | 无输出（零错误） |
| `npm test` | `✅ 全部通过：179 项` |

装到的版本：`@openai/agents 0.11.8` / `zod 4.6.5` / `openai 6.49.0` / `typescript 5.x`。

> `npm install` 如果报 `CODEBUDDY_BROKER_DENY: Brokered mkdir refused`，
> 加 `--cache ./.npm-cache` 换个 cache 目录即可。

**冒烟测试覆盖什么**（不启 EdgeOne、不连飞书、`fetch` 全被 stub）：

| 节 | 内容 |
|---|---|
| A | 官方测试向量解密、签名计算、非法密文报错 |
| B | 事件体解析、@占位符剥离、非文本消息静默忽略、challenge 识别 |
| C | 命令解析（含 `/compact` `/clear` `/memory` `/forget` `/login` `/logout`；并确认 `/repo` 已判成未知命令） |
| D | 去重窗口、限流、prune、token 缓存跨实例恢复、条数上限 |
| E | webhook 端到端：验签、解密、转发头、那个 36 字符的坑 |
| F | 回合流程：/help /status /compact /clear /memory /forget /提问，含模型失败必须发声 |
| G | 会话压缩的纯函数：历史渲染、写回形状、3 项门槛、头尾截断、**自动压缩判据 `shouldCompact`（含「不靠长度巧合」的钉子）** |
| H | 云文档授权的纯函数：链接识别、类型判定、state 签名 |
| I | 用户令牌管理器：单飞续期、不覆盖 refresh_token、失效重试 |
| J | 云文档工具族：docx / wiki / bitable / sheets 的读取与截断；**读走「应用优先、用户兜底」，建文档走「只用用户身份、不兜底」**；三个写工具（bitable 记录 / sheet 单元格 / docx 追加）+ feishu_docx_create 建文档 |
| K | 跨会话记忆：键名规范化、权限过滤、渲染预算、上限与遗忘、后端缺失降级 |
| L | 工作区仓库 ws_ls / ws_read / ws_write / ws_rm：列表、base64 解码、sha 自动解析、创建与覆盖、404 三因提示 |

**这一步曾经真的抓到过 bug**（留着当参考，说明为什么值得写）：

| 症状 | 根因 |
|---|---|
| 多行消息的**缩进被吃掉** | `extractText` 用了全局 `/[ \t]+/g`，把 `\n  第二行` 压成 `\n 第二行` |
| 十来个工具测试全返回英文错误 | `invoke(ctx, input)` 的 `input` 是 **JSON 字符串**，传对象会解析失败 |
| `switch` 上留着已删的 `case "repo"` 编译不过 | `Command` 联合里没有 `repo` 了，`case` 必须是联合成员 |
| `/repo a/b` 静默变成普通提问 | `parseCommand` 的 `default` 分支若返回 `ask`，未知命令会被送给模型即兴回答 |
| **建文档的 4 条测试区分不出用的哪个身份** | 桩函数**只按 URL 路由**，谁发的请求都接。于是把身份从「应用」改成「用户」，那 4 条一条都不会红 —— 测试和实现对同一件事没有独立判断。**已补两条带 `authorization` 断言的测试**，并用变异检验确认它们会红（2026-09-26） |

---

## 第 1 步 · 本地起服务

```bash
npm i -g edgeone     # 如果还没装
edgeone login        # ⚠️ 必须先登录，否则 dev 起不来
npm run link         # 关联 Makers 项目，同步环境变量（可选）
npm run dev
```

**预期**：控制台输出本地地址，默认 `http://localhost:8088`。

> ⚠️ 没登录时 CLI 会直接拒绝：
> `You are not authenticated, and browser login is unavailable in a non-interactive environment.`
> 非交互环境里改用 `-t <token>` 或 `EDGEONE_PAGES_API_TOKEN` 环境变量。
> 令牌可以从 `~/.edgeone/<sha256>`（JSON，取 `value.Token`）里捞出来复用。

> ⚠️ **本机有代理变量**，会拦 localhost 请求（表现为 502 或 000）。
> 用 curl 测的时候一律加 `--noproxy '*'`。
> 冷启动约 15~20 秒，日志先空后满，别误判成崩溃。

### ⚠️ 本地 dev 的三个实测坑（2026-09-25）

1. **`agent-node` 不绑定端口**。日志停在 `[agent-node] Observability: openai-agents`，
   `9006` 一直不监听，只有 `node-function`(9005) 和 `observability`(9101) 起来。
   **`/feishu` 这条 agent 路由本地打不到** —— 要么部署后测，要么按第五节的办法
   在 Node 里直接 `import .edgeone/agent-node/server.mjs` 调运行时。
2. **前端 dev server 会被跳过**，提示
   `The configured dev command would recurse into edgeone itself.` ——
   因为 `package.json` 的 `dev` 就是 `edgeone makers dev`。
   所以「打开 `http://localhost:8088/` 看状态页」这一步**本地验不了**，要部署后验。
3. **加 `--skip-env-sync --skip-ai-gateway-sync`**，否则会卡在交互式提问
   `? Sync environment variables from the EdgeOne Makers console? › (y/N)`，
   非交互环境下表现为「命令挂住不动」。

> ⚠️ dev server 会在后台**重写 `.edgeone/agent-node/node_modules`**。
> 在它运行期间去 `import` 那个目录会拿到 `Cannot find package '.../@openai/agents-core'`
> 这种**假错误**。要先杀掉 dev server 再 import。

---

## 第 2 步 · 【未知数 1】后台执行能力 ⭐ 最关键

> ✅ **2026-09-25 线上已实测通过 —— 结果是 A。**
> `{"phase":"finished","elapsedMs":15046,"verdict":"✅ 后台执行可用 …"}`。
> `FEISHU_DISPATCH_MODE` 保持 `async`。下面是复验步骤，换域名/改运行时后重跑。

在状态页点「**跑一次自测**」按钮。它做两件事：

1. `POST /feishu?probe=start` → 立刻返回，后台睡 15 秒后写标记
2. 等 16 秒 → `POST /feishu?probe=check` → 读标记

命令行复验（`?probe=start` / `?probe=check` 是**故意公开**的，不加令牌 ——
状态页要在浏览器里直接点它，把 `INTERNAL_TOKEN` 塞进静态页等于公开它）：

```bash
CID="probe-$(date +%s | tail -c 7)"
curl -s --noproxy '*' -X POST -H "Makers-Conversation-Id: $CID" \
  "https://eolab.yujizi.org/feishu?probe=start"
sleep 17
curl -s --noproxy '*' -X POST -H "Makers-Conversation-Id: $CID" \
  "https://eolab.yujizi.org/feishu?probe=check"
```

⚠️ 两次请求**必须带同一个 `Makers-Conversation-Id`** —— 标记是写在会话 state 里的，
换个 id 就查不到，`phase` 会一直显示 `started`，看着像「后台不可用」，其实是查错了地方。

**预期结果 A（✅ 后台可用）**：

```json
{
  "phase": "finished",
  "verdict": "✅ 后台执行可用 —— 用默认的 FEISHU_DISPATCH_MODE=async"
}
```

→ `FEISHU_DISPATCH_MODE` 保持 `async`。

**预期结果 B（❌ 后台不可用）**：

```json
{
  "phase": "started",
  "verdict": "❌ 后台任务没跑完（响应发出后进程被回收了）—— 把 FEISHU_DISPATCH_MODE 改成 sync"
}
```

→ 把 `FEISHU_DISPATCH_MODE` 改成 `sync`，重新部署。

**为什么要 15 秒**：太短的话任务可能在响应发出之前就跑完了，测出来的是
「同步能跑」而不是「后台能跑」—— 假的通过。

命令行等价做法：

```bash
CID=probe-abc123

curl --noproxy '*' -s -X POST "http://localhost:8088/feishu?probe=start" \
  -H "Makers-Conversation-Id: $CID"

sleep 16

curl --noproxy '*' -s -X POST "http://localhost:8088/feishu?probe=check" \
  -H "Makers-Conversation-Id: $CID"
```

---

## 第 3 步 · 本地模拟一条飞书事件

**先确认 `.env` 里没配 `FEISHU_ENCRYPT_KEY`**（不配就跳过验签，好测）。

```bash
curl --noproxy '*' -s -X POST http://localhost:8088/feishu-webhook \
  -H 'content-type: application/json' \
  -d '{
    "header": { "event_type": "im.message.receive_v1", "token": "你配的-VERIFICATION-TOKEN" },
    "event": {
      "sender": { "sender_type": "user", "sender_id": { "open_id": "ou_test" } },
      "message": {
        "message_id": "om_test_1",
        "chat_id": "oc_0123456789abcdef0123456789abcdef",
        "chat_type": "p2p",
        "message_type": "text",
        "content": "{\"text\":\"/help\"}"
      }
    }
  }'
```

**预期**：

- webhook 返回 `{"code":0}`
- 控制台里看到 agent 收到转发
- 发消息那一步会失败（`tenant_access_token` 取不到，除非凭证是对的）——
  **这是正常的**，本地没有真实飞书应用时本来就发不出去

**这一步验证的是**：检查顺序对不对、conversation_id 有没有生成成功、
转发链路通不通。不是验证「消息能不能发到飞书」。

### 顺手验一下那个 36 字符的坑

```bash
# 上面的 chat_id 是 35 字符，拼 fs- 会变 38 —— 看 webhook 是否仍然返回 200
# 如果 agent 返回 400，说明 conversation_id 生成那一步没走哈希
```

看 agent 的响应（webhook 失败时会带上 `detail`）：应该是 `{"ok":true,...}`，
不是 `{"error":"缺少 Makers-Conversation-Id 请求头"}`。

---

## 第 4 步 · 【未知数 2】流事件的实际名字

本地起服务后打开：

```
http://localhost:8088/agent-metrics
```

先随便问一个简单问题（比如直接发 `你好`，但这一步需要真实模型凭证）。

**要看的是**：`raw_model_stream_event` 的 `data.type` 都有哪些值。

| 你看到的 | 说明 |
|---|---|
| `output_text_delta` | ✅ 和代码里一致，答案增量正常 |
| 别的名字 | 改 `agents/feishu/_host.ts` 的 `answerDelta()` |
| 有 reasoning 相关的事件 | 把实际名字填进 `reasoningDelta()` |

**对不上的后果**：卡片上不显示增量（只有最终答案），或者不显示思考/工具那一串。
**不影响答案正确性** —— 最终答案优先取 `result.finalOutput`。

同时顺便确认一下**平台工具实际注册的名字**（`commands` / `code_interpreter` /
`web_search` / `files_*`），和 `_tools.ts` 的 `EXCLUDED` 列表对得上。

---

## 第 5 步 · 真实飞书链路

1. 配好 `FEISHU_ENCRYPT_KEY`（现在该配上了）
2. 按 `docs/02-飞书配置.md` 走完飞书后台配置，**记得发版本**
3. 私聊 bot：

| 发什么 | 预期 |
|---|---|
| `/help` | 收到命令列表（**不该**出现 `/repo`） |
| `/status` | 报「长期记忆 N 条」+ 云文档授权状态 |
| `现在金价多少？` | 先搜网页再答，带来源链接 |
| `/repo sindresorhus/is-stream` | 「不认识的命令 /repo」—— **不能**变成一段模型即兴回答 |
| 再发一次**同样**的问题 | 正常回答（不同 message_id，不该被去重） |
| `/compact` | 「已压缩：N 条历史 → 摘要（约 M 字）」 |
| 压缩后问 `我们刚才聊了什么` | **能从摘要答出**（这条是压缩的关键验收：答不出说明写回的形状不对） |
| `/clear` | 「会话已清空…」 |
| 清空后问 `我们聊过什么` | 回答没有历史；但 `/memory` 里的长期事实**还在** |
| 空会话上发 `/compact` | 「只有 N 项历史，没什么可压缩的」 |
| **自动压缩**：连着聊到历史渲染超 12 万字（约 30 条满长度消息）后再问一句 | 回答**正常**，卡片末尾多一行「历史过长，已自动压缩成摘要（N 条 → 约 M 字）」。⚠️ 提示必须在**回答之后**出现 —— 压缩不能拖慢回答本身 |
| 上一条之后问 `我们刚才聊了什么` | **能从摘要答出**（同 `/compact` 那条的关键验收） |
| 短会话（例如只有几轮）之后继续问 | **不该**出现自动压缩提示 —— 判据只看「渲染有没有超 12 万字」，**不看条数**。压了反而更长 |
| `/agent-metrics` 看 `compactWanted` / `autoCompactions` | 两个都涨 = 判据和压缩都工作；只有前者涨 = 判据说该压但没压成（多半摘要返回空，诊断里有 `auto-compact-skipped`） |
| `/login` | 收到一条授权链接（链接必须原样出现，不走模型） |
| 授权后贴一条 docx 链接并问内容 | 读出正文；`/status` 里授权状态变成已授权 |
| **不授权**直接贴一份共享给机器人的文档 | 也读得出来（应用身份先行，2026-09-25 起） |
| 让它往多维表格加一条记录（共享时勾了可编辑） | 表里多一条；卡片工具行显示「新增 1 条记录」 |
| 让它往文档末尾补一段 | 文档末尾多出该段；要改已有内容它应说明只能追加 |
| 说 `写一份报告，放进飞书文档`（**先 `/login` 授权过**） | 它**自己建**一篇新文档并给出链接，不要求你先建好；卡片工具行显示「已建文档并写入 N 段」。**打开后应该能改、能删** —— 这是用户身份的验收点 |
| 说 `写一份报告，放进飞书文档`（**没授权**） | 直说需要先 `/login`，**不建**。⚠️ 绝不能偷偷退成应用身份建一篇出来（那样用户只读、谁也删不掉）。见 `docs/01` 的取舍说明 |
| `/logout` | 「已撤销云文档授权。」 |
| `/memory` | 还没记过东西时，说明「记忆是空的，可以让我记 …」，**不是一片空白** |
| 说 `记住：这个项目用 pnpm 和 TypeScript` | 回复里应出现「记下了：…」（便于当场纠正） |
| `/memory` | 列出 `prefs/技术栈`（或模型自己起的键名） |
| `/forget 技术栈` | 「已忘记 …」。**不带 `prefs/` 前缀也要能删掉** |
| `/forget 不存在的键` | 直说找不到，不是静默成功 |

> ✅ **2026-09-26 线上实测：建文档 → 用户可编辑，全通。**
>
> 1. 用 `node tools/ask-bot.mjs` 让机器人真建了一篇 → 拿到链接，正文 3 段都写进去了
> 2. 用 **ego 浏览器以用户身份**打开那篇文档 → 工具栏是「**编辑**」，**不是**「申请编辑权限」
> 3. 往正文里打了一段字 → **真的落进正文**，状态栏变成「已经保存到云端」
>
> 对照 2026-09-25 应用身份建的那篇：同样操作**打字打不进**、菜单是「申请编辑权限」、
> 删除回「无权限操作」。所以这次改动确实把「拿到一份自己改不了的报告」
> 变成了「一份自己的报告」。
>
> ⚠️ 途中踩到一个**授权范围**的坑，值得单记：授权还在、令牌也在、续期也正常，
> 但建文档回 `99991679 required [docx:document, docx:document:create]`。
> 原因是那几个写 scope 是**后加**的，而**刷新令牌不会带来新 scope** ——
> 必须重新走一次完整 `/login`（本地 5 分钟能修好，但不知道的人会去翻代码）。
> 代码里已把这条翻成能照着做的提示；`oauth.ts:28` 早有这条注释，只是没进报错文案。

4. **验去重**：在飞书后台的推送日志里点「重新推送」同一条消息 ——
   bot 不该再回答一次。

5. **验自环防护**：bot 发「正在抓取…」这类回执时，
   那条回执自己会作为事件推回来 —— 不该触发新一轮对话。

### 验跨会话记忆（这一层的验收必须**换一个会话**）

`context.store.state` 是按会话隔离的，所以「在同一个群里记得」**证明不了**跨会话记忆生效 ——
那可能只是平台自己的会话状态。真正的验收是**换一个 conversation_id**。

⚠️ **会话键是 `Makers-Conversation-Id` 请求头，不是 `chat_id`。**
（2026-09-25 实测修正：此前这里写的是「不同 chat_id → 不同 conversation_id」，**错的**。）
同一个真实单聊、同一个 `chat_id`，只要 `Makers-Conversation-Id` 不同，
平台侧就是**不同实例、不同会话、不同 state**。所以验收反而更干净：
`chat_id` 恒定，唯一的变量就是那个头 —— 得出来的结论排除了「其实是聊天记录在起作用」。

**已自动化：`npm run verify:memory`**（`tools/verify-memory.mjs`，走飞书 API 端到端，7 个阶段）

> ✅ **2026-09-25 线上实测：13/13 全绿 —— 跨会话记忆成立。**
> 关键三行：会话 B 在记忆为空时答不出 → 会话 A 记下后 B 答得出「青鸟-7」和发布分支
> → 全新会话 D 在清理后答不出。`/clear` 之后 A 仍然记得（说明记忆和对话历史是两片空间）。

| 步骤 | 预期 |
|---|---|
| 阶段 1 · 会话 B **基线**：先问一次，且记忆是空的 | **答不出**，且明说不知道 ← 证明这个事实不是猜得出来的 |
| 阶段 2 · 会话 A：`记住：内部代号是「青鸟-7」…` | 回执出现「记下了」 |
| 阶段 3 · 回到会话 B 复问（唯一变量是共享记忆） | 答得出「青鸟-7」+ 发布分支 ← **这条才是关键** |
| 阶段 4 · 会话 C（第三个，从没参与过）首次问 | 也答得出 |
| 阶段 4.5 · 会话 C **连问两次** | 两次答案一致，**不许横跳** |
| 阶段 5 · 用飞书 API 读 `/memory` | 读得到那条事实 |
| 阶段 6 · 会话 A 发 `/clear` 后再问 | **仍然答得出**（`/clear` 只清对话历史，不动长期记忆） |
| 阶段 7 · `/forget all confirm` → `/memory` → **会话 D** 复查 | 说「还没有任何长期记忆」；D 不再断言该事实 |

**四条踩过的坑，别重踩：**

1. **事实必须不可猜。** 第一版用「这个项目用 pnpm 还是 npm」当探针 ——
   模型光靠先验就能答对，基线那步直接失效，**整个验证等于没做**。
   现在用的是「青鸟-7」「release/2026q4-migration」这种编造出来的、不可猜的值。
2. **判「答出来了」不能用 `includes(keyword)`。** 模型会**自我更正**：
   它会说「『青鸟-7』是我编的 / 没有任何来源」，这句话里**带着关键词但在否认**。
   必须先判有没有否认词（`更正 / 我编的 / 查不到 / 不知道…`），再判有没有事实。
3. **conversation_id 每轮都要带 nonce。** 用固定 id 的话，第二轮的「基线」会继承
   上一轮留下的会话历史，模型会把上轮看到过的事实复述出来 → 基线假失败。
4. **清理后的复查必须用「没聊过这条事实」的全新会话。**
   这一条踩得最久：原本在会话 C 里复查（C 在阶段 4/4.5 已经答过这条事实），
   清理后 C 照样答得出来，还补一句「实时确认过 `prefs/项目代号`，没变」——
   看着像「清理没生效」。实际是**从自己的对话上文里复述的**，跟长期记忆无关；
   而且那句「实时确认过」是**编的**（它压根没调 `recall_facts`）。
   换成全新会话 D 立刻正常。顺带：模型对自己行为的不实陈述已在提示词里堵上
   （`_instructions.ts` 的读数规则 ③）。

> 如果阶段 1 都过不了（基线就说出了事实），先打 `?probe=store` 看 Blob 后端在不在 ——
> 记忆功能在后端拿不到时会**整体静默关闭**（不挂工具、提示词也不提），
> 表现就是「bot 完全不知道自己有记忆」，而不是报错。

---

## 第 6 步 · 部署

推到 Git 仓库让 Makers 自动构建，或：

```bash
npm run deploy
```

⚠️ **两个部署的坑（都实测踩过）**：

1. **`deploy` 后面不要带路径参数**。`edgeone makers deploy .` 会跳过本地构建、
   把整个源码目录直接打包上传，平台侧重装依赖再构建，产物很容易超过
   「单文件 25MiB」限制（实测 30MiB 被拒）。`npm run deploy` 不带路径，
   CLI 会本地构建进 `.edgeone/` 并只上传构建产物（平台日志显示
   `Project only contains .edgeone directory, skipping build` 才是对的）。
2. **部署前删掉项目根的 `edgeone-agent-lab.zip`**。CLI 部署时会在项目根生成
   这个打包 zip，而它的静态资源收集不认识 `.zip`（默认只排除
   `node_modules/.cache/.git/.DS_Store/*.log/*.tmp`），下一轮构建会把它当
   静态文件收进 `.edgeone/assets/` → 31MiB 超限，从此每次部署都失败。
   `npm run deploy` 已把 `clean:zip` 串在前面。
3. **构建卡在 `Pre-build hook triggered` 不动，要 `env -u NODE_OPTIONS` 跑**。
   症状：日志停在

   ```
   🔍 [Logger Plugin] Pre-build hook triggered
   📂 Working directory: /Users/guci/work/edgeone-agent-lab
   ```

   然后十几二十分钟没有新输出，`.edgeone/` 的 mtime 也不动。
   原因不是网络也不是构建慢，而是环境里注入的 safe-delete shim
   （通过 `NODE_OPTIONS=--require=.../node-language-shim.cjs`）在构建清理
   `.edgeone/`（上万个文件）时弹 `SAFE_DELETE_BULK_CONFIRM_REQUIRED`，
   非交互模式下没人确认，就**一直挂着**。

   正确写法（`dangerouslyDisableSandbox` 单独用不够，shim 还在 NODE_OPTIONS 里）：

   ```bash
   env -u NODE_OPTIONS npm run deploy
   ```

   顺带把代理变量也摘掉（`-u HTTP_PROXY -u HTTPS_PROXY -u http_proxy -u https_proxy -u ALL_PROXY -u all_proxy`），
   免得本机代理失效时把 CLI 的请求也带走。

⚠️ **部署会重置 `AI_GATEWAY_*`**。日志里那行
`[cli] Bound AI Gateway credentials to project ...` 就是它干的 ——
这三个键会被平台值覆盖。自定义模型配置放 `OPENCODE_*`。

**部署后要重跑第 2 步**（`https://<域名>/feishu?probe=start` / `?check`）——
线上的实例回收策略可能和本地不一样，本地通过不代表线上通过。

---

## 诊断探针（部署后随时可用）

排查「bot 不回 / 答不上来」时，**不要靠发飞书消息试**——一轮十几秒，
而且只能看到被包装过的错误文案。直接打探针，每一层单独测。

除 `start` / `check` 外，其余探针都要带 `token=<INTERNAL_TOKEN>`（这几个端点是公网可达的）；
`start` / `check` 不挡令牌是**故意的** —— 状态页要在浏览器里直接点它们，
把令牌塞进静态页等于公开它。所有探针都**必须带 `Makers-Conversation-Id` 头**，
否则平台先回 400。

> ⚠️⚠️ **探针是 GET，边缘会缓存 —— 复验时必须加 cache-buster。**
> 实测踩过：部署成功之后打 `?probe=store`，响应里一直缺新加的字段，
> 第一反应是「构建没生效 / 部署没上去」，差点去翻构建日志。
> 加上 `&_=$(date +%s)` 立刻就有了。
> `-H "Cache-Control: no-cache"` **没用**（那是请求头，边缘不认），必须让 URL 变。
> 凡是「改完代码 → 打探针复验」的流程都要带它，否则会把缓存误诊成部署失败。

```bash
BASE=https://eolab.yujizi.org/feishu
TOK=<INTERNAL_TOKEN>
CID=probe-diag-0001        # 任意 6~36 位 [0-9a-zA-Z-_.]
h=(-H "Makers-Conversation-Id: $CID")
T=$(date +%s)              # ← cache-buster，别省

curl -s "${h[@]}" "$BASE?probe=env&token=$TOK&_=$T"    | python3 -m json.tool
curl -s "${h[@]}" "$BASE?probe=model&token=$TOK&_=$T"  | python3 -m json.tool
curl -s "${h[@]}" "$BASE?probe=search&token=$TOK&_=$T" | python3 -m json.tool
curl -s "${h[@]}" "$BASE?probe=store&token=$TOK&_=$T"  | python3 -m json.tool
```

⚠️ 这四个探针**都**要求 `token=$TOK`（`INTERNAL_TOKEN`）。`?probe=env` 在
2026-09-25 之前**没有**这道门 —— 它是泄漏最多的一个（`INTERNAL_TOKEN` 和
`SERPER_API_KEY` 的长度 + 首尾、serper key 的 FNV 指纹、env 变量名清单），
而 agent 路由是公网可达的，实测无令牌也能拿到 200。现在五个探针的门禁收口在
`tokenGate()` 一处。**唯一公开的是 `?probe=start` / `?probe=check`**（状态页
要在浏览器里直接点，理由见第 2 步）。

| 探针 | 测的是哪一层 | 看什么 |
|---|---|---|
| `?probe=env` | 环境变量**读到了没有** | **`envSource`（第一个看这个）**、`baseUrl`、`model`、`opencodeSession`、`managedAiGateway` |
| `?probe=model` | 模型请求**发得出去吗** | `upstream` 是哪个网关、`envSource`；200 就是通 |
| `?probe=search` | 运行时**出网**吗 | serper 的原始状态码和结果标题 |
| `?probe=store` | **跨会话存储**在不在 | **`memory.available` 和 `memory.reason`（第一个看这个）**、`runtime.getStoreType`、`storageEnv.PAGES_BLOB_DEPLOY_CREDENTIAL.isUnreplacedPlaceholder` |

> `memory.reason` 把「记忆为什么不见了」直接说成人话 —— 只有两种病：
> 运行时没暴露 `getStore`（平台改了 API），或者拿不到 projectId。
> 它和真实回合走的是**同一个解析函数**（`memoryBackendStatus`），
> 所以这个字段说 available 就是真 available。
> 顺带看 `memory.projectIdFrom` —— 能告诉你到底是 `env.PAGES_PROJECT_ID`
> 还是 `process.env.ProjectId` 生效了（这两个是不同来源，排查时容易混）。

`?probe=store` 还有一个跨会话的实测用法（**要换一个 conversation_id**）：

```bash
# 会话 A 写
curl -s -H "Makers-Conversation-Id: probe-a-0001" \
  "$BASE?probe=store&op=write&token=$TOK" | python3 -m json.tool

# 会话 B 读 —— blob.foreignCount > 0 就证明裸 Blob 跨会话可见
curl -s -H "Makers-Conversation-Id: probe-b-0002" \
  "$BASE?probe=store&op=read&token=$TOK" | python3 -m json.tool
```

写入都在 `probe-*` 前缀下，不碰 `repo.snapshot` / `feishu.*` 那些真键，也不碰
平台的 `memory-<projectId>` 里任何非探针键。

**为什么 `env` 和 `model` 要分开**：`env` 只证明变量读到了，不证明请求发得出去。
这两个探针各自对应一类完全不同的病：

| 探针结果 | 病在哪 |
|---|---|
| `envSource` 是 `AI_GATEWAY_*` | **路由退回了兜底**：`OPENCODE_*` 没配全。这是最常见的一种 |
| `env` 里 `baseUrl` 是 `ai-gateway.edgeone.link` | 同上，或者 `OPENCODE_*` 被删了 |
| `model` 401 `API key not found` | **打错门**：key 是 A 家的，baseURL 指着 B 家 |
| `model` 400 `provider prefix` | 模型名规则不对（EdgeOne 要 `@makers/`，OpenCode 要裸名） |
| `model` 429 / `QUOTA_EXHAUSTED` | 额度 |
| `model` 200 但 `search` 失败 | 模型没问题，查 serper key / 出网 |
| 两个都 200 但 bot 还是答不上来 | 才轮到看提示词和工具描述 |

⚠️ **`AI_GATEWAY_*` 是平台托管的，改不动**。每次 `edgeone makers deploy` 都会
`Bound AI Gateway credentials to project`，把这三个键覆盖回平台值。
所以自定义的模型配置必须放在 `OPENCODE_*` 这类普通键里。
`managedAiGateway` 字段就是用来看「它是不是又被重置了」。

⚠️ 加新环境变量时，记得同步 `probeEnv` 里的过滤正则
（`/FEISHU|INTERNAL|AI_|AGENT|SANDBOX|SERPER|OPENCODE/`）。
漏一个就会让那个变量在探针里**隐身**——曾经漏了 `SERPER`，
误判成「环境变量没生效」，白排查了半天。

---

## 一张表：出问题先看哪

| 现象 | 去哪看 |
|---|---|
| 类型错误 | 第 0 步的 `npm run typecheck` |
| 路由 404 | 文件位置：`agents/feishu/index.ts` → `/feishu`，`cloud-functions/feishu-webhook/index.ts` → `/feishu-webhook` |
| 401 内部令牌不匹配 | `INTERNAL_TOKEN` 在 webhook 和 agent 两边是不是同一个 |
| 503 没有配置 INTERNAL_TOKEN | 环境变量没配。这是**故意的** fail-closed |
| 400 缺少 Makers-Conversation-Id | webhook 的转发头没带上，或直接访问了 `/feishu` |
| 飞书收不到任何回复 | 第 5 步的排错顺序表 |
| bot 回「没答上来：…」 | 先打 `?probe=model`。200 才轮到查提示词 |
| 搜索不生效 | `?probe=model` 和 `?probe=search` 各打一次，分清是模型层还是工具层 |
| 回答像是编的、没有来源 | 提示词里「先搜再答、标来源」那段被改了，或模型没调 `web_search`。先打 `?probe=model` 排除模型层 |
| 换了群 / 换个人就「不记得」 | 先打 `?probe=store` 确认后端在；再看渲染的权限过滤（`user/*` 只注入提问人自己的） |
| bot 完全不知道自己有记忆 | **后端拿不到时功能整体静默关闭**（不挂工具、提示词也不提）。看 `?probe=store` 的 `runtime.getStoreType` |
| `/memory` 回「拿不到持久存储」 | 同上。这是设计如此，不是 bug |
| 记忆写不进去了 | 撞了 `MAX_ENTRIES = 100`。提示词里应让模型先 `forget_fact` 再记 |
| 卡片一直停在「生成中」 | 卡片权限没配，或 `closeStreaming` 失败。`streamer.finish()` 里已吞掉异常，不影响已显示的内容 |
