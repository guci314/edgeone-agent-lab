# ─────────────────────────────────────────────────────────────────────
# 复制成 .env 后填。.env 已在 .gitignore 里。
#
# 线上部署时这些要配在 Makers 控制台的「环境变量」里，不是这个文件。
# `edgeone makers link` 可以把控制台的环境变量同步到本地调试。
# ─────────────────────────────────────────────────────────────────────

# ── 模型：OpenCode Go（当前部署用的就是这套）──────────────────────────
# ⚠️ 用 OPENCODE_* 这组名字，**不要**用 AI_GATEWAY_* —— 那三个是平台托管的，
# 每次 `edgeone makers deploy` 都会被 CLI 的
# `Bound AI Gateway credentials to project` 覆盖回平台值，
# 手写的值活不过一轮部署（实测：setEnvs 写成功、listEnvs 回读一致，部署完就变回去）。
#
# ⚠️ 模型名**必须写裸名**。OpenCode Go 不认带前缀的名字（会 404）。
# key 和 baseURL 必须**成对**配 —— 只配一半会被当成没配（见 resolveModelRoute）。
OPENCODE_API_KEY=sk-你的-openCode-go-密钥
OPENCODE_BASE_URL=https://opencode.ai/zen/go/v1
OPENCODE_MODEL=deepseek-v4.1-flash

# OpenCode Go 要求带 x-opencode-session 头，缺了会被直接拒。
# 值只是个路由标签，随便取，但同一个项目内保持一致。
OPENCODE_SESSION=edgeone-agent-lab

# ── 兜底：EdgeOne AI Gateway（平台托管，免费档 50 万 token）────────────
# 代码在 OPENCODE_* 没配全时会退回这组。但**别指望能改它**：deploy 会重置。
# ⚠️ 这家的模型名规则是**反的**：必须带 provider 前缀，裸名会 400
#    `Model ID must include provider prefix`。
#
# AI_GATEWAY_API_KEY=sk-部署时自动写入
# AI_GATEWAY_BASE_URL=https://ai-gateway.edgeone.link/v1
# AI_GATEWAY_MODEL=@makers/deepseek-v4.1-flash
#
#   免费档（平台代付）：@makers/<model>
#     可用：@makers/deepseek-v4.1-flash、@makers/deepseek-v4-pro、
#           @makers/minimax-m3、@makers/kimi-k2.6、@makers/hy3
#   自带 key：<provider>/<model>，provider ∈ openai | anthropic | google |
#           deepseek | minimax | zai | moonshot | hunyuan

# ── 飞书自建应用 ───────────────────────────────────────────────────
FEISHU_APP_ID=cli_xxxxxxxxxxxx
FEISHU_APP_SECRET=xxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx
# 事件订阅页的 Verification Token
FEISHU_VERIFICATION_TOKEN=xxxxxxxxxxxxxxxxxxxxxxxx
# 加密策略里的 Encrypt Key。**强烈建议配** —— 不配就没有任何请求来源证明，
# 任何人都能伪造事件推送。见 src/feishu/crypto.ts 的说明。
FEISHU_ENCRYPT_KEY=xxxxxxxxxxxxxxxxxxxxxxxx

# ── 内部调用令牌 ───────────────────────────────────────────────────
# 飞书 webhook（cloud-functions）转发给 agent（agents/feishu）时带的自制头。
# agents 路由是公网可达的，没有这道门任何人都能拿它烧你的模型额度。
# 生成：openssl rand -hex 24
INTERNAL_TOKEN=请换成随机串

# ── 调度模式 ───────────────────────────────────────────────────────
# async（默认）：agent 先回 202，再在后台跑完整个回合。webhook 立刻 ACK 飞书。
# sync         ：agent 跑完才返回。webhook 要等，但**不依赖后台执行能力**。
#
# 为什么会有这个开关：EdgeOne 的 agents/ 运行时会话粘性、实例保活 300 秒，
# 但「返回 Response 之后未 await 的异步代码还能不能跑完」官方没有明文承诺。
# 部署后先用 /feishu/agent 上的 ?probe=1 实测（见 docs/03-验证清单.md），
# 实测结果决定这里填哪个。
FEISHU_DISPATCH_MODE=async

# ── 云文档授权（OAuth，读飞书文档 / 知识库 / 多维表格 / 电子表格）────
# 回调地址。**必须和飞书后台「安全设置 → 重定向 URL」里登记的、以及
# cf-agent-lab 的路由完全一致**，差一个字符授权页就直接报错。
# ⚠️ 故意没有默认值：猜一个的话用户看到的是飞书的一个报错页，完全不知道原因。
FEISHU_OAUTH_REDIRECT_URI=https://agent.yujizi.org/api/feishu/oauth

# 需要用户在飞书后台「权限管理」里开的 scope（不开的话授权页会报错）：
#   docx:document:readonly     新版文档只读
#   wiki:wiki:readonly         知识库只读（含 get_node 解引用）
#   bitable:app:readonly       多维表格只读
#   sheets:spreadsheet:readonly 电子表格只读
#   offline_access             ← **必须有**，否则拿不到 refresh_token，
#                                用户每两小时要重新授权一次
# 改完权限要**发一个新版本**才生效，不发版配了也没用。

# ── 可选：续期测试开关 ─────────────────────────────────────────────
# 默认提前 5 分钟判 access token 过期，意味着想验一次真实的续期得等将近两小时。
# 集合成 7200000（2 小时）后，随便读一次文档就会走到续期分支。
# **平时不要设。**
# FEISHU_USER_TOKEN_SKEW_MS=7200000

# ── 可选：无头测试通道（tools/ask-bot.mjs）───────────────────────────
# 本机直接给 agents 路由投合成事件、并把回复读回来，**不经过飞书网页版**。
# 为什么需要它：飞书网页版必然索要通知 / 本地网络权限，而 Chromium 刻意不让
# 脚本点这类弹窗，于是「发一条消息试试机器人」这个动作每次都卡在人工点确认上。
#
# 用法：npm run ask -- "读一下这个表格 <链接>，哈尔滨的销量是多少"
#
# 这个 open_id 是**提问人**在该应用命名空间下的 id —— 用户令牌按 openId 分槽
# （见 src/feishu/user-token.ts），填错人就会一路报「未授权」。
# 拿法：让机器人回一条消息，发 /status 看它显示的那串（`ou_…`）。
# FEISHU_DEBUG_OPEN_ID=ou_xxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx

# ── 可选：沙箱开关 ─────────────────────────────────────────────────
# 平台内置沙箱工具（commands / code_interpreter / files_* / browser_*）。
# 设成 0 / false / off 就整条不挂给模型。
SANDBOX_ENABLED=1

# ── 可选：GitHub 工作区（ws_ls / ws_read / ws_write / ws_delete）──
# 不配就这四个工具一个都不挂 —— 挂出来只会让模型反复试一个必然失败的工具。
# GITHUB_TOKEN=github_pat_xxx
# GH_WORKSPACE_REPO=owner/repo
# GH_WORKSPACE_BRANCH=main
